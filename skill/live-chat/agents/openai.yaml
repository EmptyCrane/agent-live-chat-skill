interface:
  display_name: "Live Chat"
  short_description: "Stream real multi-agent conversations in a local browser"
  default_prompt: "Use $live-chat to run a goal-driven conversation with real subagents, configured roles, tones, model preferences, rounds, and completion state."
