interface:
  display_name: "Live Chat"
  short_description: "Stream and replay persistent multi-agent conversations"
  default_prompt: "Use $live-chat to stream a goal-driven conversation with real subagents, preserve it as a session, and export or replay the history."
